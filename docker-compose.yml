version: "3.8"

services:
  client:
    container_name: car-booking-app_client
    env_file:
      - .env
    build:
      context: ./client
    ports:
      - "80:80"
  server:
    container_name: car-booking-app_server
    build:
      context: ./server
    ports:
      - "8080:8080"
